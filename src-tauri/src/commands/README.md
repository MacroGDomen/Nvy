# Tauri Commands

This directory is reserved for Tauri command modules.

Rust-side code should focus on desktop capabilities such as filesystem access, paths, SQLite access, Stronghold, and external links. Business rules should stay in the TypeScript domain layer unless there is a clear performance or security reason.

