{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Nvy window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "stronghold:default",
    "stronghold:allow-remove-store-record"
  ]
}
