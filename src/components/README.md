# Components Layer

This directory contains reusable UI components.

Current structure:

- `ui/` for low-level visual primitives.
- `layout/` for shared layout components.

Components should receive data and callbacks through props instead of owning business rules.

